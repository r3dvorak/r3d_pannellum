# TODO

- [ ] Re-run JED Checker on the rebuilt package.
- [ ] Verify the package-level `sys.ini` files are included in the ZIP.
- [ ] Confirm the package manifest is accepted without `PKG_` prefix warnings.
- [ ] Review whether Joomla's `showon` fields should remain or be replaced with a JED-safe structure.
- [ ] Publish the updated update-server XML after the final package check.
